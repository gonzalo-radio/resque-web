# Resque::Web config (Perl hash, loaded by the Mojolicious Config plugin).
# The whole hash is passed to Resque->new, so `redis` points the app at the
# redis service on the compose network. See setup_plugins in lib/Resque/Web.pm.
{
    redis => 'redis:6379',
}
